// so we have created a route class that will contains all route for our apps

class MyRoute {
  static String loginRoute = '/home';
  static String homeRoute = '/';
  static String somethingElse = '/something';
}
